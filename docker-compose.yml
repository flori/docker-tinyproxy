services:
  tinyproxy:
      image: flori303/tinyproxy:4d67082
      stdin_open: yes
      tty: yes
      restart: unless-stopped
      ports:
        - '8080:80'
      volumes:
      - ./config/tinyproxy.conf:/etc/tinyproxy/tinyproxy.conf
